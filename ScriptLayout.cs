//EVERYTHING UNDER AUTOMATICALLY COMES WITH EVERY SCRIPT OTHER THAN MY COMMENTS

//These "using" things are pretty much just importing in tools to use in this script.
//Do not have to worry about it rn
//Like if you make a house you would need be using a hammer.
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//A class is pretty much just a script, set as protectionLevel "class" name
    //This name shouldn't be camelCase, just normal Uppercase
//the : after the name means Inheriting from which means you can use things that that script has in its code. Automatically uses unities "monobehaviour", DOESNT MATTER RN
//The name MUST match the name of the file
public class ScriptLayout : MonoBehaviour
{
    //HERE IS WHERE YOU WOULD DECLARE VARIABLES

    // Start is called before the first frame update
    void Start()
    {
        //DOES THIS AT THE START OF THE GAME
    }

    // Update is called once per frame
    void Update()
    {
        //EVERY FRAME
    }

    // PUT FUNCTIONS AT THE BOTTOM
}
